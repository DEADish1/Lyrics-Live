import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'LyricsLive - AI Performance Coach for Rappers & Singers',
  description: 'Analyze your rap and vocal performances with AI-powered insights. Get bar-by-bar feedback, flow analysis, and personalized coaching.',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
